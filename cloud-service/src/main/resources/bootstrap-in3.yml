spring:
  application:
    name: cloud-service
  cloud:
    config:
      label: master
      profile: in3
      #uri: http://127.0.0.1:8888
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka