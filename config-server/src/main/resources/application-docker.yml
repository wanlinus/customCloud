eureka:
  client:
    service-url:
      defaultZone: http://10.0.0.108:8000/eureka
  instance:
    prefer-ip-address: true
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            classpath:repo/dev/,classpath:/repo/dev2/
          add-label-locations: false
#        git:
#          uri: https://github.com/wanlinus/CustonConfig
#          search-paths: dev2
#          default-label: master
server:
  port: 8000