eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka
spring:
  application:
    name: ribbon-server
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server