spring:
  application:
    name: zuul-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      label: master
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka
